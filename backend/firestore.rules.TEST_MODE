rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ⚠️ TEST MODE - HERKESİN OKUMA/YAZMA İZNİ VAR!
    // Production'da MUTLAKA değiştir!
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

